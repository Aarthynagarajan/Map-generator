spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  ai:
    google:
      genai:
        chat:
          options:
            model: gemini-1.5-flash
            temperature: 0.1

logging:
  level:
    com.processmap: DEBUG
    org.springframework.security: INFO
