spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  ai:
    openai:
      chat:
        options:
          model: gpt-4o
          temperature: 0.1

logging:
  level:
    com.processmap: DEBUG
    org.springframework.security: INFO
