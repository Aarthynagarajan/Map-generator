spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/processpro
    username: admin
    password: dev_only
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  ai:
    openai:
      api-key: mock-test-key
